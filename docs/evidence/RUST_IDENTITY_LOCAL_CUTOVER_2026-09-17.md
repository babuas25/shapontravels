# Local Rust identity activation — 17 September 2026

The user authorized continuing cutover on localhost, selected **Jewel Babu** as the first Rust Super Admin and instructed that the old unresolved booking did not need resolution for this identity cutover. This authorization was restricted to localhost. No provider account deletion, production activation, financial import or supplier transaction was performed.

## Active target

- Frontend: `http://localhost:3000`; Rust: `http://127.0.0.1:18081`.
- PostgreSQL: loopback `55439`, database `portal_local_20260917_identity_test`, migrations through `0046`.
- Rollout: `2ad572a2-f1e4-4b52-ad96-e04efa2d7188`, target `localhost_identity_20260917`, revision `3`, state `active` (initial activation was revision 1; removal-review UI update used pause 2 / resume 3).
- Backend release fingerprint: `ad1f273b2496a5d2b755d40440996a6c7fff453509ccf7b1225f8f3dc1828a13`.
- Frontend release fingerprint: `9bdde512f55014115bd56f6821c549b10ce8f79c8c322070083295225194585d`.
- Frontend identity/wallet: `rust-canonical`; Clerk authentication retained. Provider adapter uses the existing Clerk **test** instance. Temporary operator credentials are absent from the serving configuration.

The database suffix enables the local test-provider boundary; **this populated database is not disposable**. Do not run empty-database test fixtures, cleanup/reset commands or legacy launchers against it.

## Data preservation and mapping

The original migration-33 database `api_portal_local_clerk_20260914` is retained unchanged. Its complete custom-format backup was restored independently and all 47 original tables and sequences matched. Source fingerprints were checked again after stopping the old local writers; they were unchanged.

That backup was restored into the selected target. Additive migrations installed identity tables. An explicit, audited transaction mapped six provider-verified active Clerk subjects, retaining their three Super Admin and three B2B roles. Three new agencies received zero-balance BDT wallets; two existing agency client IDs were linked to their exact subjects' wallets. No old wallet balances or financial history existed to import. Two additional subjects retained by old clients were independently confirmed absent from Clerk and stored as denied tombstones, with no new login or privilege. All existing client IDs, booking/draft owners, passenger data, supplier configuration and booking evidence were retained.

Only migration records and the new wallet rows/links changed among the 47 old tables before serving. Once browser checks ran, ordinary request rate counters also advanced. No retained business row was rewritten. All four bookings remain: three `held`, one `outcome_unknown`.

The populated canonical target was backed up and independently restored into `portal_restore_20260917_identity_test`; all **79 tables and all sequences matched**. Backup SHA-256:

`f9d360257ce49f24b69ddac276e2027eea3aed6d85cde1fc74e0fa24bdfc88eb`

Original retained-UAT backup SHA-256:

`d3d125cb12a4bea8c718d1767c6265a5111595a09585513e8d6e5213947e5079`

## Activation and checks

The original Next and Rust processes were stopped. The selected Rust process started in maintenance with the current release pin and a temporary operator capability. Fresh preflight reported zero mapping issues and only the known unresolved booking. Independent backup/restore, shutdown and configuration artifacts were bound into the rollout plan. `identity-rollout.mjs apply` confirmed revision 1 as active; the serving process then restarted without operator credentials and with maintenance disabled.

The explicit retained-booking acknowledgment is only accepted on loopback `_identity_test` databases. Other work queues must be empty, mapping issues zero and all retained unresolved bookings old `outcome_unknown` rows. Booking state/update time is bound into the reviewed mapping digest and checked under locks. Pending/recent outcomes remain blocked. No booking was marked resolved; deletion dependency protection is unchanged.

Verified with the actual existing Chrome/Clerk login:

- Users & Roles resolves Jewel Babu as Super Admin, showing six active accounts plus two historical tombstones.
- An account without bookings opens a removal review successfully. The confirmation checkbox/button was not submitted.
- A booking-linked agency instead shows `booking_references`, without the former Supabase security-service error.
- Booking Management displays all four retained bookings, including the original unknown supplier outcome.
- Wallet Accounts shows three active wallets, BDT 0.00 available and held, and no pending review.
- Clerk read-only recheck still lists the same six active accounts and roles.
- Final DB check: zero create, invitation, deletion, provider-effect or mail operations; zero funded wallets. No account/provider mutation was generated by verification.
- Rust readiness: schema/bootstrap/canonical ready; maintenance false; provider adapter enabled; exactly one unresolved work item, the retained booking. All other queues are empty. Health readiness returns 200.

Regression verification passed: expanded isolated rollout activation/pause/resume matrix (including retained-booking age/state, other-backlog rejection, drift and concurrency); preflight CLI contract; Next canonical-authority and security-lock suites; rustfmt and strict Clippy for library, tests and local identity example; local Rust example build.

## Remaining boundaries and restart

### Removal review visibility correction

The user reported that clicking **Review removal** appeared ineffective. The existing page had rendered the response below the directory, creation form and invitations, and displayed the internal `booking_references` code. The account the user selected was correctly blocked by retained bookings, but the interaction did not explain this clearly.

`components/identity/Accounts.tsx` now opens a focused, accessible native dialog immediately, identifies the selected account, shows loading/error feedback in the dialog and explains each dependency in plain language. Blocked accounts link to Booking Management or Pending operations where applicable. Eligible accounts retain an unchecked confirmation and disabled removal button. Close/Escape restores focus to the triggering button. After an explicitly confirmed preparation, the operation status is brought into view. No deletion rule or provider operation changed.

TypeScript and targeted ESLint passed. Actual signed-in browser checks covered the immediate loading dialog, booking-blocked account, eligible account with disabled confirmation, Close, Escape/focus restoration and visible service-failure feedback. No checkbox/confirmation was submitted and no account was removed.

A controlled local pause/resume updated the frontend pin: pause revision 2, active revision 3. A fresh paused-target backup was restored independently; all 79 tables and sequences matched (SHA-256 `026740ef501c5634ec773085c622fd048e69f6d3d89630e6d7159bd2e78e0474`). Fresh preflight had zero mapping issues and only the previously acknowledged unknown booking. The new runtime is canonical-ready with maintenance disabled and no operator credentials. Private evidence: `.local/identity-removal-review/20260917T061757Z/`.

### External setup

Identity mail delivery and inbound identity event delivery are not enabled. SMTP credentials exist, but the canonical mail callback currently requires HTTPS; localhost is HTTP. No real mail, invitation or webhook was sent as a test. A separately reviewed external-delivery setup is still needed. Existing Cloudinary configuration is explicitly retained for normal asset functionality; no upload/deletion occurred. Production cutover and production operational validation remain outside this local activation.

`scripts/portal-dev.mjs` now detects the private canonical configuration and delegates to `identity-local-dev.mjs`. Startup resumes the selected authority; it does not reimport, migrate or reactivate. [Restart instructions](../LOCAL_PORTAL.md).

Private credential/data artifacts are in `.local/identity-local-cutover/20260917T054910Z/` and `.local/identity-local/config.json` (0600, Git-ignored). These include source/import manifests, both backups, restore evidence, preflight/plan/result, readiness and browser/data checks. Do not copy these private files into source control or chat. After activation, recover forward using maintenance and pinned pause/resume; do not restore stale legacy authority by toggling environment flags.
